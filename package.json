{
  "name": "de-giphy-ier",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "watch": "esbuild ./src/index.ts --bundle --platform=node --packages=external --outdir=dist --watch",
    "start": "nodemon ./dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "nodemon": "^3.1.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "puppeteer-core": "^22.6.1"
  }
}
